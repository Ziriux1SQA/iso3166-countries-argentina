/**
 * Entry point para el script de seed de ubicaciones
 * 
 * Uso: pnpm seed
 * 
 * Este archivo importa y ejecuta el módulo de seed modular
 * ubicado en ./seed/
 */

import { runSeed } from "./seed";

runSeed();
